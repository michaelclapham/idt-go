# InDesign Translation Library (written in Go)

This is a command line tool / library for translating InDesign files.

# Run locally

Run locally using ```go build && ./idt-go [COMMAND] [FILENAME]```
For example
```go build && ./idt-go extract test.idml```